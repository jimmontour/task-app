import React, { Component } from 'react'

export default class SubmitTasks extends Component {
   render() {
      return (
         <div>
            <input type="text" />
            <button>Add Task</button>
         </div>
      )
   }
}
